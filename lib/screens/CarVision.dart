import 'package:flutter/material.dart';

class CarVision extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Car Vision'),
      ),
      body: Center(
        child: Text('Car Vision'),
      )
    );
  }
}
